<% for student in students %>
	<div class="student-item well" id="student-<%= student.id %>">
		<i class="icon-user"></i>
		<h2><%= student.name %></h2>
		<%= link_to "X", student, confirm: "Are you sure?", method: :delete, remote: true %>
	</div>
<% end %>