<% provide(:title, "My Classes") %>
	<h3 align="center">나의 학급 <%= link_to "새 수업 등록", "#user_class_modal", 
	{ class: "btn btn-mini btn-primary", "data-toggle" => "modal" } %></h3>
<hr >
	<%= render partial: "modal" %>
<div class="row">
	<ul class="user_classes">
		<%= render @user_classes %>
	</ul>
</div>
