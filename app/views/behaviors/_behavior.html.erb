<ul class="well" id="<%= behavior.id %>">
	<% if behavior.score > 0 %>
		<i class="icon-thumbs-up"></i>
	<% else %>
		<i class="icon-thumbs-down"></i>
	<% end %>
	<%= behavior.title %>
	<%= link_to 'X', behavior_path(behavior), confirm: "Are you sure?", method: :delete, remote: true %>
</ul>