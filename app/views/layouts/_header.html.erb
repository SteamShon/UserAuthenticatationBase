<header class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <%= link_to "Teacher Note", root_path, id: "logo" %>
      <nav>
        <ul class="nav pull-right">
          <% if signed_in? %>
            <li><%= link_to "나의 학급", user_classes_path %></li>
            <li><%= link_to "최근 활동", '#' %></li>
            
            <li id="fat=menu" class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                Account <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to "Profile", current_user %></li>
                <li><%= link_to "Settings", edit_user_path(current_user) %></li>
                <li class="divider"></li>
                <li>
                  <%= link_to "로그 아웃", signout_path, method: "delete" %>
                </li>
              </ul>
            </li>
          <% else %>
            <li><%= link_to "로그 인", signin_path %></li>
          <% end %>
          <li><%= link_to "Help", '#', onclick: "alert(문의사항은 shom83@hotmail로 메일주세요.);" %></li>
        </ul>
      </nav>
    </div>
  </div>
</header>
